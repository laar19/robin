services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: robin-frontend-dev
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - VITE_APP_NAME=Robin
    command: npm run dev -- --host 0.0.0.0
